var a = 42;

var b = (a > 41) ? "hello" : "world";

/*
var name = (conditional) ? if true : if false

This assigns one of two possible values
to a variable depending on the outcome
of a conditional.

*/